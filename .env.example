# Optional — enriches dictionary hits with US/UK usage, examples, and a fact.
# Without a key, public-dictionary lookups still work.
XAI_API_KEY=
# Fallback if you prefer OpenAI instead of xAI/Grok
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
